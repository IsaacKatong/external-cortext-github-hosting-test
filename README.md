# Local Storage

This folder holds external storage data locally as an alternative to GitHub. It provides a simpler setup for users who do not need remote storage. This is also the storage used for initial testing.

Data files in this folder (other than this README) are ignored by git.
